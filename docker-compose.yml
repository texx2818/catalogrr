services:
  catalogrr:
    build:
      context: ./backend
    container_name: catalogrr
    ports:
      - "4120:4120"
    volumes:
      - catalogrr-data:/data
    restart: unless-stopped
    environment:
      - PORT=4120
      - DB_PATH=/data/catalogrr.db

volumes:
  catalogrr-data:
